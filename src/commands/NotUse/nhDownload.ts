import { Client, Message } from "@open-wa/wa-automate";

export = {
    name:"nhdownload",
    description: "download on nhenti using code/name",
    execute: async (Message: Message, args: string[],client:Client) => {
    }
}